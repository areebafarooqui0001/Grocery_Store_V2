Frontend: 

# To install node modules : npm install

# To run the vue application: npm run dev

Backend:

# Path to the project folder: cd /mnt/c/Users/user/Desktop/grocerymart/GroceryMart

# To run redis server: redis-server

# To run the application: python3 main.py

# To run: mailhog

# To start the celery worker: celery -A main.celery_app worker -l info

# To start the celery beat: celery -A main.celery_app beat --max-interval 1 -l info
